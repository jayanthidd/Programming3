package org.campus02.zam.ss2020.threadsandmultithreading;

public class ThreadSrtartsTwice {
    // one thread cannot run more than once simulatenously.  We must have more threads declared and cannot run the same thread more than once.
}
